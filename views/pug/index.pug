extends main

block content
  main 
    button(onclick="location.href = '/productos'") Agregar productos
    section
      h1="Lista de Productos"
      each element in productos
        article 
            img(src=element.imagen, alt="")
            h3=element.title 
            p= element.price
